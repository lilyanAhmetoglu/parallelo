{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Extension",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}",
        "${env:HOME}/Desktop/parallelo-lab/repo"
      ],
      "outFiles": ["${workspaceFolder}/out/**/*.js"]
    }
  ]
}
